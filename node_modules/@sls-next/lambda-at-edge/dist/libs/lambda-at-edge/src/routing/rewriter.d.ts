import { RoutesManifest } from "../../types";
export declare function getRewritePath(path: string, routesManifest: RoutesManifest, router: (uri: string) => string | null, normalisedPath: string): string | null;

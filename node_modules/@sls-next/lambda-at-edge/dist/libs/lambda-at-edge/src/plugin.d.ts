import type { ServerlessComponentInputs } from "./types";
import { ServerlessContext } from "@serverless/core";
export declare type DeploymentResult = {
    appUrl: string;
    bucketName: string;
    distributionId: string;
};
declare class LambdaAtEdgePlugin {
    inputs: ServerlessComponentInputs;
    context: ServerlessContext;
    constructor(inputs: ServerlessComponentInputs, context: {});
    build(): Promise<void>;
    deploy(): Promise<import("./deployer").DeploymentResult>;
    remove(): Promise<void>;
}
export default LambdaAtEdgePlugin;
